<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppCepeRio.Views.LoginView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="LoginView"
    BackgroundColor="{AppThemeBinding Dark={StaticResource Black},
                                      Light={StaticResource White}}"
    Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <!--  Entry colors  -->
        <Color x:Key="DarkLoginEntryColor">#121212</Color>
        <Color x:Key="LightLoginEntryColor">#eee</Color>

        <!--  Placeholder color  -->
        <Color x:Key="DarkLoginPlaceholderColor">#aaa</Color>
        <Color x:Key="LightLoginPlaceholderColor">#6f7b82</Color>

        <!--  Login Button  -->
        <Color x:Key="DarkLoginButton">#239a51</Color>
        <Color x:Key="LightLoginButton">#1ed760</Color>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Dark={StaticResource DarkLoginButton}, Light={StaticResource LightLoginButton}}" />
        </Style>

        <Style x:Key="ImageStyle" TargetType="Image">
            <Setter Property="Source" Value="{AppThemeBinding Dark=loginpic3.png, Light=lightmodelogo.png}" />
        </Style>

        <Style x:Key="BorderStyle" TargetType="Border">
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Dark={StaticResource DarkLoginEntryColor}, Light={StaticResource LightLoginEntryColor}}" />
            <Setter Property="StrokeShape" Value="RoundRectangle 13" />
            <Setter Property="Stroke" Value="Transparent" />
        </Style>

        <Style x:Key="EntryStyle" TargetType="Entry">
            <Setter Property="FontSize" Value="19" />
            <Setter Property="Margin" Value="20, 0" />
            <Setter Property="HeightRequest" Value="65" />
            <Setter Property="PlaceholderColor" Value="{AppThemeBinding Dark={StaticResource DarkLoginPlaceholderColor}, Light={StaticResource LightLoginPlaceholderColor}}" />
        </Style>
    </ContentPage.Resources>

    <VerticalStackLayout
        Padding="35"
        Spacing="10"
        VerticalOptions="Start">
        <Image
            x:Name="LoginImage"
            Margin="0,50"
            Style="{StaticResource ImageStyle}"
            WidthRequest="200" />

        <Border Style="{StaticResource BorderStyle}">
            <Entry
                Keyboard="Telephone"
                Placeholder="Matrícula/CPF"
                Style="{StaticResource EntryStyle}" />
        </Border>

        <Border Style="{StaticResource BorderStyle}">
            <Entry
                Keyboard="Password"
                Placeholder="Senha"
                Style="{StaticResource EntryStyle}" />
        </Border>

        <Button
            x:Name="LoginBtn"
            Margin="0,10"
            Clicked="LoginBtn_Clicked"
            CornerRadius="13"
            FontAttributes="Bold"
            FontSize="25"
            HeightRequest="65"
            Style="{StaticResource ButtonStyle}"
            Text="Entrar"
            TextColor="White" />



        <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
            <Label
                FontAttributes="Bold"
                FontSize="18"
                HorizontalTextAlignment="Center"
                Text="Primeira vez no app?"
                TextColor="#13b158" />
            <Label
                FontAttributes="Bold"
                FontSize="18"
                HorizontalTextAlignment="Center"
                Text="Criar senha"
                TextColor="#ddd" />
        </HorizontalStackLayout>

        <Label
            FontAttributes="Bold"
            FontSize="18"
            HorizontalTextAlignment="Center"
            Text="Esqueceu a senha?"
            TextColor="#13b158" />
    </VerticalStackLayout>
</ContentPage>